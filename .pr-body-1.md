## Overview
This PR significantly improves error handling and developer experience in the BEEP SDK.

## What Changed

### 🎯 Typed Error Hierarchy
- Created comprehensive error classes: `BeepError`, `BeepAuthenticationError`, `BeepPaymentError`, `BeepValidationError`, `BeepNetworkError`, and `BeepRateLimitError`
- Each error type corresponds to specific failure scenarios

### 🔢 Semantic Error Codes  
- Implemented structured error codes (e.g., `BEEP_1001` for invalid API key)
- Organized by category: 1xxx (auth), 2xxx (network), 3xxx (payment), 4xxx (invoice), 5xxx (validation), 6xxx (rate limit)

### 💬 User-Friendly Messages
- Added `getUserMessage()` method that returns human-readable error descriptions
- Helps developers show appropriate messages to end users

### 🔍 Enhanced Debugging
- New `BeepDebugger` class with configurable logging options
- Request/response interceptors for API call debugging
- Sensitive data sanitization in logs (API keys, passwords, etc.)
- Custom logger support for integration with existing logging systems

### 📋 Better Error Context
- Errors now include: request ID, status code, timestamp, and additional details
- `toJSON()` method for comprehensive error serialization
- Helps with troubleshooting and support tickets

### 📚 Documentation & Examples
- Added comprehensive error handling example (`examples/error-handling.ts`)
- Demonstrates error type checking, retry logic, and Express middleware patterns

## Impact

**Before:**
```typescript
throw new Error('API Error: Something went wrong');
```

**After:**
```typescript
throw new BeepAuthenticationError(
  'Invalid API key format',
  BeepErrorCode.INVALID_API_KEY,
  { hint: 'API keys should start with beep_sk_ or beep_pk_' }
);
```

## Developer Benefits
- ✅ Clearer error messages for faster debugging
- ✅ Proper error categorization for better error handling
- ✅ Built-in debugging tools reduce development time
- ✅ Consistent error structure across the SDK
- ✅ Better production error tracking and monitoring

## Breaking Changes
None - existing error handling will continue to work, but developers can now catch specific error types for better handling.

## Testing
- All existing tests should pass
- New error types are backwards compatible with generic Error catching