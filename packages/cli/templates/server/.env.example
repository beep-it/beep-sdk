COMMUNICATION_MODE=https
BEEP_API_KEY=
PORT=4071

# Required in https mode. Callers must send `Authorization: Bearer <MCP_AUTH_TOKEN>`.
# Requests are refused while this is unset.
MCP_AUTH_TOKEN=

# Comma-separated, optional.
MCP_ALLOWED_ORIGINS=
MCP_ALLOWED_HOSTS=
