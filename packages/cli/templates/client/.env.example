## Transport selection for MCP client -> seller MCP server
# Use 'https' for hosted sellers (recommended), or 'stdio' for local/embedded servers.
COMMUNICATION_MODE=https

## Full URL to the SELLER's MCP HTTP endpoint (used when COMMUNICATION_MODE=https)
# Example for companyA: https://companyA.example.com/mcp
SERVER_URL=http://localhost:3001/mcp

# Optional: API key for private endpoints if needed by your client runtime
BEEP_API_KEY=
POLL_INTERVAL_MS=15000
POLL_TIMEOUT_MS=300000
## Keep first runs simple: connect and list tools only
DEFAULT_LIST_ONLY=true
DEFAULT_TOOL_NAME=
DEFAULT_TOOL_ARGS=
