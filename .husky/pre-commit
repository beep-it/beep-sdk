#!/usr/bin/env sh

echo "Running pre-commit hooks..."

# Run lint-staged to lint and format only the changed files
pnpm lint-staged

# Also run a full test to make sure everything works together
pnpm test
